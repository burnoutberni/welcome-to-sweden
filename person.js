module.exports = function Person(firstName, lastName, position, languages, lastStep) {
    return {
        firstName: firstName,
        lastName: lastName,
        fbId,
        position: position,
        languages: languages,
        lastStep: lastStep
    }
}